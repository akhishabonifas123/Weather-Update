# Weather-Update
# 🌦️ Simple Weather Application (Day 4 Task)

A lightweight Python application that fetches real-time weather data from the OpenWeatherMap API. This project demonstrates basic API integration, JSON data parsing, and user input handling.

## 🚀 Features
* Get current temperature and weather conditions for any city.
* Supports metric units (Celsius).
* Error handling for invalid city names or network issues.
* Clean, readable console output.

## 🛠️ Prerequisites
Before running this project, ensure you have the following:
* **Python 3.x** installed on your machine.
* An **API Key** from [OpenWeatherMap](https://openweathermap.org/api).
* The `requests` library installed.
